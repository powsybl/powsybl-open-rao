{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "$id": "resource:/schemas/dangling-line-action/dangling-line-action-v2.5.json",
  "description": "Action that specifies the new active set-point of a dangling line",
  "type": "object",
  "properties": {
    "networkElementId": {
      "description": "The identifier of the network element on which the action applies",
      "type": "string"
    },
    "activePowerValue": {
      "description": "The new active power set-point of the dangling line",
      "type": "number"
    }
  },
  "additionalProperties": false,
  "required": [
    "networkElementId",
    "activePowerValue"
  ]
}
