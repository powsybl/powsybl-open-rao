{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "$id": "resource:/schemas/injection-range-action/injection-range-action-v1.4.json",
  "description": "A range action interface specifying an action on one or several Injections (Generator and/or Loads)",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier of the remedial action",
      "type": "string"
    },
    "name": {
      "description": "The name of the remedial action",
      "type": [
        "string",
        "null"
      ]
    },
    "operator": {
      "description": "The operator of the remedial action",
      "type": [
        "string",
        "null"
      ]
    },
    "groupId": {
      "description": "The group identifier of the range action. All range actions which share the same group identifier should have the same setpoint.",
      "type": [
        "string",
        "null"
      ]
    },
    "speed": {
      "description": "The time it takes to trigger the remedial action",
      "type": "number"
    },
    "networkElementIdsAndKeys": {
      "description": "A map of network element identifiers and their distribution keys",
      "type": "object",
      "additionalProperties": {
        "type": "number"
      }
    },
    "initialSetpoint": {
      "type": "number"
    },
    "ranges": {
      "description": "The list of operational ranges delimiting the bounds of the range action, if several are defined the RAO will only consider their intersection",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "max": {
            "type": "number"
          },
          "min": {
            "type": "number"
          }
        },
        "additionalProperties": false
      }
    },
    "freeToUseUsageRules": {
      "type": "array",
      "items": {
        "$ref": "../free-to-use-usage-rule/free-to-use-usage-rule-v1.0.json"
      }
    },
    "onStateUsageRules": {
      "type": "array",
      "items": {
        "$ref": "../on-state-usage-rule/on-state-usage-rule-v1.0.json"
      }
    },
    "onFlowConstraintUsageRules": {
      "type": "array",
      "items": {
        "$ref": "../on-flow-constraint-usage-rule/on-flow-constraint-usage-rule-v1.0.json"
      }
    },
    "onAngleConstraintUsageRules": {
      "type": "array",
      "items": {
        "$ref": "../on-angle-constraint-usage-rule/on-angle-constraint-usage-rule-v1.4.json"
      }
    },
    "onFlowConstraintInCountryUsageRules": {
      "description": "The list of usage rules defined on a given country, triggered when a flow CNEC of that country is constrained",
      "type": "array",
      "items": {
        "$ref": "../on-flow-constraint-in-country-usage-rule/on-flow-constraint-in-country-usage-rule-v1.3.json"
      }
    },
    "extensions": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "additionalProperties": false,
  "required": [
    "id",
    "networkElementIdsAndKeys",
    "initialSetpoint",
    "ranges"
  ]
}
